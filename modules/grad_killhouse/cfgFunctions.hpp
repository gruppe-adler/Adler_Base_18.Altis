#ifndef MODULES_DIRECTORY
    #define MODULES_DIRECTORY node_modules
#endif

class grad_killhouse {
    class builds {
        file = MODULES_DIRECTORY\grad_killhouse\functions\builds;
        class basic {};
    };
    class common {
        file = MODULES_DIRECTORY\grad_killhouse\functions\common;
        class addAction {};
        class createObj {};
        class createTarget {};
        class lockDoors {};
        class reset {};
    };
};
